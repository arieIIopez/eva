import { chromium } from 'playwright';
import fs from 'node:fs/promises';
import path from 'node:path';
const baseUrl=process.env.EVA_EXPERIMENT_BASE_URL||'http://127.0.0.1:8080/experiments/runner-population-first-only.html';const outDir=path.resolve('results/paper-population-first-only');await fs.mkdir(outDir,{recursive:true});
function esc(v){if(v==null)return'';const s=typeof v==='object'?JSON.stringify(v):String(v);return /[",\n]/.test(s)?`"${s.replaceAll('"','""')}"`:s;}async function csv(name,rows){rows=rows||[];if(!rows.length)return fs.writeFile(path.join(outDir,name),'','utf8');const keys=[...rows.reduce((s,r)=>(Object.keys(r).forEach(k=>s.add(k)),s),new Set())];await fs.writeFile(path.join(outDir,name),[keys.join(','),...rows.map(r=>keys.map(k=>esc(r[k])).join(','))].join('\n')+'\n','utf8');}
const browser=await chromium.launch({headless:true});const page=await browser.newPage();page.setDefaultTimeout(120*60*1000);page.on('console',m=>{const t=m.text();if(t.startsWith('[paper-pop-fast]')||m.type()==='error')console.log(t)});page.on('pageerror',e=>console.error('[pageerror]',e));try{await page.goto(baseUrl,{waitUntil:'domcontentloaded',timeout:120000});await page.waitForFunction(()=>window.__EVA_EXPERIMENT_READY||window.__EVA_EXPERIMENT_ERROR,null,{timeout:180000});const err=await page.evaluate(()=>window.__EVA_EXPERIMENT_ERROR||null);if(err)throw new Error(err);const result=await page.evaluate(async()=>window.EVA_PAPER_EXPERIMENTS.runPopulationFirstOnly({epsilon:0,fullPopulationReference:600177}));await fs.writeFile(path.join(outDir,'summary.json'),JSON.stringify(result,null,2),'utf8');await csv('population_first_sequence.csv',result.rows);await csv('remaining_projects.csv',result.remaining_project_ids.map(id=>({id})));console.log('POPULATION_FIRST_ONLY_SUMMARY',JSON.stringify(result.summary));}finally{await browser.close();}