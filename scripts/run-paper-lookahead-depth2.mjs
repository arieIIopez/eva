import { chromium } from 'playwright';
import fs from 'node:fs/promises';
import path from 'node:path';

const baseUrl=process.env.EVA_EXPERIMENT_BASE_URL||'http://127.0.0.1:8080/experiments/runner-lookahead-depth2.html';
const outDir=path.resolve('results/paper-lookahead-depth2');await fs.mkdir(outDir,{recursive:true});
const baseline=JSON.parse(await fs.readFile(path.join(outDir,'baseline-greedy.json'),'utf8'));
function esc(v){if(v==null)return'';const s=typeof v==='object'?JSON.stringify(v):String(v);return /[",\n]/.test(s)?`"${s.replaceAll('"','""')}"`:s;}
async function csv(name,rows){rows=rows||[];if(!rows.length)return fs.writeFile(path.join(outDir,name),'','utf8');const keys=[...rows.reduce((s,r)=>(Object.keys(r).forEach(k=>s.add(k)),s),new Set())];await fs.writeFile(path.join(outDir,name),[keys.join(','),...rows.map(r=>keys.map(k=>esc(r[k])).join(','))].join('\n')+'\n','utf8');}
function compare(seqA,seqB,key){const rb=new Map(seqB.map((x,i)=>[x,i+1])),disp=seqA.map((x,i)=>Math.abs(i+1-rb.get(x)));let first=0;for(let i=0;i<seqA.length;i++)if(seqA[i]!==seqB[i]){first=i+1;break;}const jac=k=>{const A=new Set(seqA.slice(0,k)),B=new Set(seqB.slice(0,k));let n=0;for(const x of A)if(B.has(x))n++;return n/new Set([...A,...B]).size;};return{scenario:key,first_divergence_step:first,mean_abs_displacement:disp.reduce((a,b)=>a+b,0)/disp.length,max_abs_displacement:Math.max(...disp),jaccard_top_10:jac(10),jaccard_top_20:jac(20),jaccard_top_30:jac(30),jaccard_top_50:jac(50),jaccard_top_100:jac(100),jaccard_top_124:jac(124)};}
function improvement(key,g,l){const fields=['cumulative_eval_rmc','cumulative_eval_balanceado','cumulative_eval_logit','cumulative_own_score','cumulative_own_score_discounted','mean_components','integrated_component_reduction','cumulative_population','cumulative_demand','cumulative_cyclists'];const o={scenario:key};for(const f of fields){o[`greedy_${f}`]=g[f];o[`depth2_${f}`]=l[f];o[`delta_${f}`]=Number(l[f])-Number(g[f]);o[`pct_${f}`]=Number(g[f])?100*(Number(l[f])-Number(g[f]))/Math.abs(Number(g[f])):null;}return o;}
const browser=await chromium.launch({headless:true});const page=await browser.newPage();page.setDefaultTimeout(120*60*1000);page.on('console',m=>{const t=m.text();if(t.startsWith('[paper-L2]')||m.type()==='error')console.log(t)});page.on('pageerror',e=>console.error('[pageerror]',e));
try{await page.goto(baseUrl,{waitUntil:'domcontentloaded',timeout:120000});await page.waitForFunction(()=>window.__EVA_EXPERIMENT_READY||window.__EVA_EXPERIMENT_ERROR,null,{timeout:180000});const err=await page.evaluate(()=>window.__EVA_EXPERIMENT_ERROR||null);if(err)throw new Error(err);const result=await page.evaluate(async()=>window.EVA_PAPER_EXPERIMENTS.runDepth2Only({discount:0.95,topK:3}));
 const comparisons=[],improvements=[];for(const run of result.runs){const b=baseline[run.key];comparisons.push(compare(b.sequence,run.sequence,run.key));const ls=result.summaries.find(x=>x.scenario===run.key);improvements.push(improvement(run.key,b.summary,ls));}
 const merged={...result,baseline_source:{run_id:baseline.source_run_id,artifact:baseline.source_artifact},greedy_vs_depth2:comparisons,improvements};await fs.writeFile(path.join(outDir,'summary-depth2.json'),JSON.stringify(merged,null,2),'utf8');await csv('depth2_summaries.csv',result.summaries);await csv('greedy_vs_depth2.csv',comparisons);await csv('depth2_improvements.csv',improvements);for(const run of result.runs)await csv(`${run.key}_depth2_sequence.csv`,run.rows);
 console.log('LOOKAHEAD_DEPTH2_SUMMARY',JSON.stringify({design:result.design,summaries:result.summaries,greedy_vs_depth2:comparisons,improvements}));
}finally{await browser.close();}