% EVA / EDTR — ecuaciones del manuscrito
% Fecha: 2026-09-01
% Fuente reproducible de las ecuaciones numeradas del paper.

% (1)
\[
T_p : G_t \to G_{t+1}
\]

% (2)
\[
I_t(p,q)=S^0\left(q\mid T_p(G_t)\right)-S^0\left(q\mid G_t\right)
\]

% (3a)
\[
S_t^0(p)=F\left(X_p^I, X_{p,t}^R, X_{p,t}^T; W,\Theta\right)
\]

% (3b)
\[
S_t^0(p)=S^0\left(p\mid G_t,\Omega,W,\Theta\right)
\]

% (4a)
\[
V_t(p,q)=S\left(p\mid G_t,\Omega,W,\Theta\right)+\delta\,S\left(q\mid T_p(G_t),\Omega,W,\Theta\right)
\]

% (4b)
\[
V_t(q,p)=S\left(q\mid G_t,\Omega,W,\Theta\right)+\delta\,S\left(p\mid T_q(G_t),\Omega,W,\Theta\right)
\]

% (4c)
\[
\Delta_{\mathrm{ord}}(p,q)=V_t(p,q)-V_t(q,p)
\]

% (5)
\[
G_0 \to G_1 \to \cdots \to G_k
\]

% (6)
\[
\max_{\pi}\;\sum_{t=1}^{k}\delta_t\,S\left(p_{\pi_t}\mid G_{t-1},\Omega,W,\Theta\right)
\]

% (7)
\[
\text{s.a.}\quad \sum_{t=1}^{k} C\left(p_{\pi_t}\right) \le B
\]

% (8)
\[
p_t^{\ast}=\arg\max_{p\in P_t^f} S_t(p)
\]

% (9)
\[
G_{t+1}=T_{p_t^{\ast}}(G_t)
\]

% (10)
\[
S_p=\frac{\sum_i w_i\,\hat{x}_{i,p}}{\sum_i w_i}
\]
